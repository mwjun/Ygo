<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /locatorcms/
    RewriteRule ^index\.php$ - [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /locatorcms/index.php [L]
</IfModule>
